rules_version = '2'; service cloud.firestore { match /databases/{database}/documents { match /miniGames/{gameId} { allow read: if true; allow write: if request.auth != null && request.auth.token.email_verified == true && request.auth.token.email == 'info@rgb-workshop.com'; } } }
